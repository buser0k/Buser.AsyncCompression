# Приоритетные Issues для Buser.AsyncCompression

## 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ

### Issue #1: Команды Pause/Resume/Cancel не работают в Program.cs
**Приоритет:** 🔴 Критический  
**Тип:** Bug  
**Теги:** `bug`, `critical`, `user-experience`

**Описание:**
В методе `StartKeyReaderAsync` в `Program.cs` (строки 92-103) команды Pause, Resume и Cancel только меняют заголовок консоли, но не выполняют реальных действий. В коде есть комментарии "Note: In a real implementation, we'd need to track the current job".

**Проблема:**
```csharp
case ConsoleKey.P:
    Console.Title = "Paused... Press R to resume.";
    // Note: In a real implementation, we'd need to track the current job
    break;
```

**Ожидаемое поведение:**
- Команда P должна вызывать `applicationService.PauseCompression(job)`
- Команда R должна вызывать `applicationService.ResumeCompression(job)`
- Команда X должна вызывать `applicationService.CancelCompression(job)`

**Шаги для воспроизведения:**
1. Запустить сжатие файла
2. Нажать P - сжатие должно приостановиться
3. Нажать R - сжатие должно возобновиться
4. Нажать X - сжатие должно отмениться

**Решение:**
Необходимо сохранить ссылку на текущий `CompressionJob` и передавать её в методы управления.

---

### Issue #2: CompressionService не поддерживает множественные задачи одновременно
**Приоритет:** 🔴 Критический  
**Тип:** Bug / Architecture  
**Теги:** `bug`, `architecture`, `thread-safety`

**Описание:**
`CompressionService` использует поля класса `_interrupted` и `_pauseEvent` для управления состоянием, что делает невозможным параллельную обработку нескольких задач сжатия. Если запустить две задачи одновременно, они будут влиять друг на друга.

**Проблема:**
```csharp
private readonly ManualResetEvent _pauseEvent = new ManualResetEvent(true);
private bool _interrupted = false;
```

**Последствия:**
- Невозможно сжимать несколько файлов параллельно
- Пауза одной задачи влияет на все остальные
- Отмена одной задачи отменяет все остальные

**Решение:**
Перенести состояние управления (pause/resume/cancel) в `CompressionJob` или использовать `CancellationToken` для каждой задачи отдельно.

---

### Issue #3: FileService методы не являются асинхронными
**Приоритет:** 🔴 Критический  
**Тип:** Performance / Bug  
**Теги:** `performance`, `bug`, `async`

**Описание:**
Методы `FileService` используют `Task.FromResult()` вместо реальных асинхронных операций, что блокирует поток выполнения.

**Проблема:**
```csharp
public async Task<Stream> OpenReadAsync(FileInfo file)
{
    return await Task.FromResult(System.IO.File.OpenRead(file.FullPath));
}
```

**Последствия:**
- Блокировка потоков при работе с большими файлами
- Неэффективное использование ресурсов
- Нарушение принципов асинхронного программирования

**Решение:**
Использовать `FileStream` с асинхронными операциями или `File.OpenReadAsync()` (если доступно в .NET 8).

---

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ

### Issue #4: Отсутствует регистрация ICompressionService в DI контейнере
**Приоритет:** 🟠 Высокий  
**Тип:** Architecture  
**Теги:** `architecture`, `dependency-injection`, `enhancement`

**Описание:**
В `ServiceConfiguration.cs` не зарегистрирован `ICompressionService`, хотя он используется в `CompressionApplicationService`. В тестах есть комментарий об этой проблеме.

**Проблема:**
```csharp
// Note: ICompressionService requires IProgressReporter which is not registered
// Note: CompressionApplicationService is not registered in DI container
```

**Последствия:**
- Нарушение принципов DI
- Невозможность легко тестировать компоненты
- Прямое создание зависимостей в `Program.cs`

**Решение:**
Добавить регистрацию `ICompressionService` и `CompressionApplicationService` в DI контейнер.

---

### Issue #5: CompressionJobFactory жестко закодирован на .gz расширение
**Приоритет:** 🟠 Высокий  
**Тип:** Enhancement  
**Теги:** `enhancement`, `flexibility`

**Описание:**
`CompressionJobFactory` всегда добавляет расширение `.gz`, независимо от используемого алгоритма сжатия.

**Проблема:**
```csharp
var outputFile = new FileInfo(inputFilePath + ".gz");
```

**Последствия:**
- Невозможно использовать другие алгоритмы сжатия (LZ4, Brotli и т.д.)
- Нарушение принципа Open/Closed (OCP)

**Решение:**
Использовать `ICompressionAlgorithm.FileExtension` для определения расширения выходного файла.

---

### Issue #6: Отсутствует использование CancellationToken
**Приоритет:** 🟠 Высокий  
**Тип:** Enhancement  
**Теги:** `enhancement`, `cancellation`, `async`

**Описание:**
Асинхронные методы не принимают `CancellationToken`, что затрудняет корректную отмену операций.

**Последствия:**
- Невозможность корректной отмены длительных операций
- Неэффективное использование ресурсов
- Нарушение best practices для async/await

**Решение:**
Добавить `CancellationToken` параметры во все асинхронные методы.

---

### Issue #7: Неэффективное ожидание в цикле ProcessCompressionAsync
**Приоритет:** 🟠 Высокий  
**Тип:** Performance  
**Теги:** `performance`, `optimization`

**Описание:**
В методе `ProcessCompressionAsync` используется `Task.Delay(1)` в цикле ожидания, что создает излишнюю нагрузку на CPU.

**Проблема:**
```csharp
while (!buffer.Post(postData))
{
    await Task.Delay(1); // Wait until buffer can accept data
}
```

**Последствия:**
- Высокая нагрузка на CPU
- Неэффективное использование ресурсов
- Возможные проблемы с производительностью

**Решение:**
Использовать `SemaphoreSlim` или другие механизмы синхронизации для более эффективного ожидания.

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### Issue #8: Отсутствует валидация размера файла перед сжатием
**Приоритет:** 🟡 Средний  
**Тип:** Enhancement  
**Теги:** `enhancement`, `validation`, `user-experience`

**Описание:**
Нет проверки размера файла перед началом сжатия. Пользователь может попытаться сжать файл, который не поместится на диск.

**Решение:**
Добавить проверку доступного места на диске и размера исходного файла перед началом сжатия.

---

### Issue #9: Отсутствует обработка ошибок при записи в выходной файл
**Приоритет:** 🟡 Средний  
**Тип:** Bug  
**Теги:** `bug`, `error-handling`

**Описание:**
В `ProcessCompressionAsync` нет обработки исключений при записи в `outputStream`. Если диск заполнится или произойдет другая ошибка, исключение может быть потеряно.

**Решение:**
Добавить try-catch блоки и корректную обработку ошибок с обновлением статуса `CompressionJob`.

---

### Issue #10: CompressionJob не имеет статуса Failed
**Приоритет:** 🟡 Средний  
**Тип:** Enhancement  
**Теги:** `enhancement`, `error-handling`

**Описание:**
В enum `CompressionStatus` есть статус `Failed`, но нет метода для установки этого статуса в `CompressionJob`.

**Решение:**
Добавить метод `Fail()` в `CompressionJob` и использовать его при ошибках.

---

### Issue #11: Отсутствует логирование
**Приоритет:** 🟡 Средний  
**Тип:** Enhancement  
**Теги:** `enhancement`, `logging`, `observability`

**Описание:**
В проекте отсутствует логирование операций, ошибок и важных событий. Это затрудняет отладку и мониторинг.

**Решение:**
Добавить структурированное логирование с использованием Serilog или Microsoft.Extensions.Logging.

---

### Issue #12: Нет поддержки множественных алгоритмов сжатия
**Приоритет:** 🟡 Средний  
**Тип:** Feature  
**Теги:** `feature`, `enhancement`

**Описание:**
В проекте реализован только GZip алгоритм. Согласно CHANGELOG, планируется поддержка LZ4, Brotli, Zstandard.

**Решение:**
Реализовать дополнительные алгоритмы сжатия и механизм выбора алгоритма.

---

### Issue #13: CompressionAlgorithmFactory не используется
**Приоритет:** 🟡 Средний  
**Тип:** Code Quality  
**Теги:** `code-quality`, `refactoring`

**Описание:**
В проекте есть `CompressionAlgorithmFactory`, но он не используется в коде. Алгоритм создается напрямую через DI.

**Решение:**
Либо использовать фабрику для создания алгоритмов, либо удалить её, если она не нужна.

---

### Issue #14: Отсутствуют unit-тесты для CompressionService
**Приоритет:** 🟡 Средний  
**Тип:** Testing  
**Теги:** `testing`, `coverage`

**Описание:**
В проекте есть интеграционные тесты, но отсутствуют unit-тесты для `CompressionService` с моками зависимостей.

**Решение:**
Добавить unit-тесты для всех методов `CompressionService`.

---

### Issue #15: FileInfo.Delete() нарушает принцип Value Object
**Приоритет:** 🟡 Средний  
**Тип:** Architecture  
**Теги:** `architecture`, `ddd`

**Описание:**
`FileInfo` является Value Object, но имеет метод `Delete()`, который изменяет состояние файловой системы. Это нарушает принципы DDD.

**Решение:**
Переместить операцию удаления в `IFileService`.

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ

### Issue #16: Отсутствует документация XML комментариев ✅ ИСПРАВЛЕНО
**Приоритет:** 🟢 Низкий  
**Тип:** Documentation  
**Теги:** `documentation`, `enhancement`  
**Статус:** ✅ Исправлено

**Описание:**
В коде отсутствовали XML комментарии для публичных API, что затрудняло использование библиотеки.

**Решение:**
Добавлены XML комментарии для всех публичных классов, методов и свойств:
- Все интерфейсы (`ICompressionAlgorithm`, `ICompressionService`, `IFileService`, `IProgressReporter`)
- Доменные сущности (`CompressionJob`, `CompressionStatus`)
- Value Objects (`CompressionSettings`, `FileInfo`)
- Application Services (`CompressionApplicationService`, `CompressionResult`)
- `ProgressBar` класс

**Изменения:**
- Добавлены XML комментарии с описанием классов, методов, параметров и исключений
- Улучшена документация для IntelliSense и генерации документации

---

### Issue #17: Hardcoded значение BlockSize в Program.cs ✅ ИСПРАВЛЕНО
**Приоритет:** 🟢 Низкий  
**Тип:** Code Quality  
**Теги:** `code-quality`, `refactoring`  
**Статус:** ✅ Исправлено

**Описание:**
В `Program.cs` использовалась константа `BlockSize = 1024 * 8`, которая дублировала значение по умолчанию из `CompressionSettings`.

**Решение:**
Удалена константа `BlockSize` и используется `CompressionSettings.Default` для получения настроек по умолчанию.

**Изменения:**
```csharp
// Было:
private const int BlockSize = 1024 * 8;
var settings = new CompressionSettings(BlockSize);

// Стало:
var settings = CompressionSettings.Default;
```

---

### Issue #18: Отсутствует проверка на существование выходного файла перед удалением ✅ ИСПРАВЛЕНО
**Приоритет:** 🟢 Низкий  
**Тип:** Code Quality  
**Теги:** `code-quality`, `optimization`  
**Статус:** ✅ Исправлено

**Описание:**
В `CompressionApplicationService.CompressFileAsync` проверялось существование файла перед удалением, но это уже делалось в `FileService.DeleteAsync`.

**Решение:**
Упрощен код, убрана дублирующая проверка. `FileService.DeleteAsync` уже обрабатывает несуществующие файлы корректно.

**Изменения:**
```csharp
// Было:
if (await _fileService.ExistsAsync(job.OutputFile, job.CancellationToken))
{
    await _fileService.DeleteAsync(job.OutputFile, job.CancellationToken);
}

// Стало:
await _fileService.DeleteAsync(job.OutputFile, job.CancellationToken);
```

---

### Issue #19: ProgressBar использует устаревший Timer API ✅ ИСПРАВЛЕНО
**Приоритет:** 🟢 Низкий  
**Тип:** Code Quality  
**Теги:** `code-quality`, `modernization`  
**Статус:** ✅ Исправлено

**Описание:**
`ProgressBar` использовал `System.Threading.Timer`, который является устаревшим API. Лучше использовать `PeriodicTimer` из .NET 6+.

**Решение:**
Обновлен на использование `PeriodicTimer` для .NET 8. Это более современный и эффективный подход для периодических операций.

**Изменения:**
- Заменен `System.Threading.Timer` на `PeriodicTimer`
- Использован `CancellationTokenSource` для корректной отмены
- Улучшена обработка ресурсов при dispose
- Добавлены XML комментарии для класса

---

### Issue #20: Отсутствует поддержка сжатия папок ✅ ИСПРАВЛЕНО
**Приоритет:** 🟢 Низкий  
**Тип:** Feature  
**Теги:** `feature`, `enhancement`  
**Статус:** ✅ Исправлено

**Описание:**
Реализована поддержка сжатия папок с рекурсивной обработкой всех вложенных файлов.

**Решение:**
- Добавлен метод `CompressionApplicationService.CompressDirectoryAsync`, выполняющий рекурсивное сжатие каталога
- Добавлены классы `DirectoryCompressionResult` и `FileCompressionSummary` для детализированного отчета о результатах
- Обновлен `Program.cs`: теперь путь к директории автоматически распознается и сжимается
- Добавлены интеграционные тесты `DirectoryCompressionIntegrationTests`
- Обновлены документация и версии проекта

---

### Issue #20: Отсутствует поддержка сжатия папок
**Приоритет:** 🟢 Низкий  
**Тип:** Feature  
**Теги:** `feature`, `enhancement`

**Описание:**
Согласно CHANGELOG, планируется поддержка сжатия папок и рекурсивной обработки.

**Решение:**
Реализовать функциональность сжатия директорий с рекурсивной обработкой.

---

## 📊 Сводка по приоритетам

- **Критический:** 3 issues (✅ все исправлены)
- **Высокий:** 4 issues (✅ все исправлены)
- **Средний:** 8 issues (✅ все исправлены)
- **Низкий:** 5 issues (✅ все исправлены)

**Всего:** 20 issues  
**Исправлено:** 20 issues  
**Отложено:** 0 issues

## 🎯 Рекомендации по порядку исправления

1. **Сначала:** Issues #1, #2, #3 (критические баги)
2. **Затем:** Issues #4, #5, #6, #7 (высокий приоритет)
3. **Далее:** Issues #8-15 (средний приоритет)
4. **В конце:** Issues #16-20 (низкий приоритет, улучшения)

